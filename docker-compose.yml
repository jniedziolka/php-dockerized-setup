version: '3.9'
services:
  server:
    build:
      context: .
      dockerfile: ./nginx/nginx.Dockerfile
    ports:
      - '8080:80'
    volumes:
      - ./:/var/www/html
  php:
    build:
      context: .
      dockerfile: ./php/php.Dockerfile
    volumes:
      - ./:/var/www/html